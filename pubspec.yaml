name: odm_ui
description: OnDaMenu Merchant POS Service

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  # original: sdk: ">=2.7.0 <3.0.0"
  # latest:
  sdk: ">=3.1.2 <4.0.0"
  # sdk: ">=2.12.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter


  # original
  # latest:
  cupertino_icons: ^1.0.2
  # original: cupertino_icons: ^0.1.3

  # firebase dependencies
  # origingl: firebase_core: "0.7.0"
  # latest:
  firebase_core: ^2.15.1
  # base git: firebase_core: "0.5.0"

  # original: firebase_auth: "^1.1.0"
  # latest:
  firebase_auth: ^4.9.0
  firebase_auth_web: ^5.8.0

  http: ^1.1.0
  http_parser: ^4.0.0
  # base git: firebase_auth: "^0.18.0+1"

  # google fonts
  # original: google_fonts: ^1.1.2
  # base git:
  google_fonts: ^6.0.0

  # flutter_svg:
  flutter_svg: ^2.0.7
  fl_chart: ^0.63.0
  provider: ^6.0.3

  # cloud firestore_ui:
  # base git: cloud_firestore: "^0.14.0+2"
  # cloud_firestore: "^0.14.1"
  # latest:
  cloud_firestore: ^4.9.1

  # BLoC Data Seperation
  flutter_bloc: ^8.0.0

  # Equatable
  equatable: ^2.0.3


dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/

